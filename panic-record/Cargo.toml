# See src/lib.rs for why this crate is named after the one it replaces.
[package]
name = "panic-probe"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
cortex-m = "0.7.7"
embassy-nrf = { version = "0.11", default-features = false, features = ["nrf52840", "unstable-pac"] }
heapless = "0.8"
